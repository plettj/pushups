# Josiah's Pushup Website
---

# Installing and Deploying

`npm run dev`: Local deployment.
`npm run build`: Build the production version.
`firebase deploy --only hosting`: Deploy the production build.
